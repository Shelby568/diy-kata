const { booleanToWord } = require("../src");

describe("booleanToWord", () => {
  
  xit("returns Yes when passed true", () => {
    expect(booleanToWord(true)).toBe('Yes')
    expect(booleanToWord(false)).toBe('No')
  });





});
