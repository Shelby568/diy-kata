const humanCatDogYears = number => {
 const a = 4;
 const b = 5;
 const remNum = number - 2;
 
 let newCat = number + 15 + 9 + remNum * a;
 let newDog = number + 15 + 9 + remNum * b;
 return arr = [number, newCat, newDog];


};

module.exports = humanCatDogYears;
